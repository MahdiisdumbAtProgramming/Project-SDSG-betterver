document.getElementById("back").onclick = function () {
    location.href = "../../load.html"; // Redirects to the parent directory and then to load.html
};
document.getElementById("backa").onclick = function () {
    location.href = "../run.html"; // Redirects to the parent directory and then to load.html
};